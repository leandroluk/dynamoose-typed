pre-commit:
  parallel: true
  commands:
    typecheck:
      run: pnpm tsc
    lint:
      run: pnpm lint

pre-push:
  parallel: true
  commands:
    typecheck:
      run: pnpm tsc
    lint:
      run: pnpm lint
    test:
      run: pnpm test:coverage
