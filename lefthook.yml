pre-commit:
  parallel: false
  commands:
    typecheck:
      run: pnpm tsc
    lint:
      run: pnpm lint

pre-push:
  parallel: false
  commands:
    typecheck:
      run: pnpm tsc
    lint:
      run: pnpm lint
    test:
      run: pnpm test:coverage
